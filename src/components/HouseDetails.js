import React from 'react'

const HouseDetails = (props) => {
    const { house } = this.props
    return (
        <div>
            <h4 className="text-center">house.name</h4>
        </div>
    )
}

export default HouseDetails
